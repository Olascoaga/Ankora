# Ankora bootstrap compatibility pointer

The canonical project contract is [`CODEX_BOOTSTRAP.md`](CODEX_BOOTSTRAP.md).

This filename is retained only because early session prompts referenced it. It intentionally does not duplicate the contract. Before modifying Ankora, every implementation or review agent must read `CODEX_BOOTSTRAP.md` completely and then follow `AGENTS.md`.
